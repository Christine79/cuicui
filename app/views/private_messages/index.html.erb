<p id="notice"><%= notice %></p>

<h1>Potins</h1>

<table>
  <thead>
    <tr>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @private_messages.each do |private_message| %>
      <tr>
  <div class="card border-primary mb-3" style="max-width: 20rem;">
    <div class="card-header">potin °         <%= private_message.id %>  </div>
    <div class="card-body">
      <h4 class="card-title">
              <%= private_message.content%>
              <%= private_message.content2%></h4>
      <p class="card-text">
        <%= link_to 'More...', private_message %>
        <%= link_to 'Edit', edit_private_message_path(private_message) %>
        <%= link_to 'Delete', private_message, method: :delete, data: { confirm: 'Are you sure?' } %>


      </p>
    </div>
  </div>






      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Private Message', new_private_message_path %>
